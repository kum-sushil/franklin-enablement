import { createOptimizedPicture } from '../../scripts/lib-franklin.js';

export default function decorate(block) {
  /* change to ul, li */
  const banner = document.querySelector('franklin-banner');
  $(".franklin-banner:nth-child(2)")

}